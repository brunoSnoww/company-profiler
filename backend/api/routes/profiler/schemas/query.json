{
  "type": "object",
  "$id": "schema:query",
  "required": ["url", "model"],
  "additionalProperties": false,
  "properties": {
    "url": {
      "type": "string",
      "format": "uri"
    },
    "model": {
      "type": "string",
      "enum": ["google", "openai"]
    }
  }
}
